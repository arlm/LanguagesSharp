namespace enquanto.Model
{
    public enum BinaryOperator
    {
        ADD,
        SUB,
        MULTIPLY,
        DIVIDE,
        AND,
        OR,
        GREATER,
        LESSER,
        EQUALS,
        DIFFERENT,
        CONCAT
    }
}
